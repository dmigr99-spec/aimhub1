# Release Channels

This directory will contain small machine-readable pointers such as `stable.json` and `beta.json` after the loader flow is implemented and tested.

A channel pointer may change. The release artifact it references must be immutable.

Do not publish a channel manifest until the corresponding release artifact exists and its hash has been verified.

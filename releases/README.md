# Immutable Releases

Versioned public client artifacts will live below this directory, for example `releases/v1.2.3/aimhub.lua`.

Stable release artifacts are immutable. If a bug is found, publish a new patch version instead of rewriting the old file.

Each published release should be traceable to a private source commit and accompanied by an artifact hash in the channel/release metadata.
